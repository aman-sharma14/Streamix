server:
  port: 8081

spring:
  config:
    import: "optional:file:.env[.properties]"
  application:
    name: identity-service
  datasource:
    url: jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres
    username: postgres.qhemejaizwfsdchbjitp
    password: EEYZoX0Zne7bDclG
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update # Crucial: This creates the tables for you automatically!
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/