server:
  port: 8083 # New Port!

spring:
  application:
    name: interaction-service
  data:
    mongodb:
      uri: ${MONGODB_URI}
      database: streamix


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/